<?xml version="1.0" encoding="utf-8"?>
<!--
  Autorise le trafic HTTP en clair vers les émulateurs Firebase locaux.
  Ce fichier vit sous src/debug/ uniquement : il n'est JAMAIS inclus dans
  un build release (voir android/app/src/debug/AndroidManifest.xml, qui
  seul le référence). Nécessaire pour integration_test/app_test.dart :
  Android bloque le cleartext HTTP par défaut depuis l'API 28, mais les
  émulateurs Firestore/Auth ne parlent qu'en HTTP sur 10.0.2.2 (alias de
  l'hôte depuis un émulateur Android) / localhost.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
